{
  "name": "songbook",
  "private": true,
  "scripts": {
    "postinstall": "(cd gui && npm install) && (cd scripts && npm install)",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^6.0.1"
  }
}
