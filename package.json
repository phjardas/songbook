{
  "name": "songbook",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix gui install",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^6.0.1"
  }
}
